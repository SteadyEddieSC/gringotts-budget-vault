# Quality Gates

A release is promotable only when the exact final head passes every required gate.

## v127 required gates

- parser and static syntax for inherited, v126 runtime, and v127 interaction-policy modules;
- browser-free v126 coordinator contracts plus v127 action-policy and ten-release-roadmap contracts;
- Chromium, Firefox, and desktop WebKit;
- Android Chromium, iPad WebKit, and iPhone WebKit;
- keyboard and visual contracts;
- axe accessibility for action hierarchy, status feedback, retained runtime diagnostics, and the v127–v136 roadmap;
- Lighthouse budgets;
- public-repository security and full-history privacy/secret scanning;
- dependency review and high/critical npm audit;
- supply-chain checks and CodeQL;
- exact-head Cloudflare preview;
- unresolved review-thread check.

## Release assertions

- current version is v127 in both HTML shells, runtime metadata, tests, and package metadata;
- v126 remains the only coordinator for rendered-route enhancement readiness;
- one live `MutationObserver` remains owned by the v126 coordinator;
- the v126 dispatcher continues to own specialist capture actions and existing current-download contracts;
- v127 adds action-intent metadata, feedback, focus behavior, dialog labeling, table-region labeling, progressive disclosure, and responsive polish without adding a second runtime;
- native table semantics remain intact;
- route-heading focus occurs only after deliberate navigation and does not override focus moved elsewhere;
- dialog focus restoration targets only a still-connected opener;
- the in-app and repository roadmap contain exactly ten releases from v127 through v136;
- only `gringottsBudgetVault.latest` may contain transaction copies;
- workbook labels, tests, and generated output remain capped at 43 sheets;
- existing export schemas and v126-owned filenames remain unchanged in this presentation-only release;
- no storage write, new financial feature, remote endpoint, service worker, automatic financial action, or additional observer is introduced;
- empty-vault protection, backup-first broad writes, immutable close history, guarded import and restore, and stable v105 rescue remain unchanged.
