# Quality Gates

A release is promotable only when the exact final head passes every required gate.

## v129 required gates

- locked dependency installation before parser validation;
- strict TypeScript no-emit checking for v128 portable-vault and v129 workflow-evidence contracts;
- parser and static syntax for inherited runtime modules, the v128 shell, the v129 workflow model, and the v129 bootstrap;
- browser-free workflow inventory, validation, privacy rejection, summary, sanitized-bundle, and filename contracts;
- Chromium, Firefox, and desktop WebKit;
- Android Chromium, iPad WebKit, and iPhone WebKit;
- keyboard and visual contracts;
- axe accessibility for the desktop and mobile Workflow Review surfaces plus retained household areas;
- unchanged Lighthouse request and byte budgets;
- public-repository security and full-history privacy/secret scanning;
- dependency review and high/critical npm audit;
- supply-chain checks and CodeQL;
- exact-head Cloudflare preview;
- unresolved review-thread check.

## Release assertions

- current version is v129 in both HTML shells, runtime metadata, tests, package metadata, and lockfile metadata;
- TypeScript 5.9.2 remains pinned and strict typechecking is required before browser installation;
- v126 remains the only coordinator for rendered-route enhancement readiness;
- one live `MutationObserver` remains owned by the v126 coordinator;
- the v126 dispatcher continues to own specialist capture actions and existing v126-owned download contracts;
- v127 remains the retained interaction, focus, action-language, table-region, dialog, and responsive presentation policy;
- v128 remains the typed portable-vault foundation and adds no cloud adapter in v129;
- v129 adds one secondary Tools tab while retaining exactly six primary destinations;
- the workflow inventory contains exactly ten bounded workflows across all six primary destinations;
- review state is kept only in module memory and is cleared by reload;
- Workflow Review does not read the authoritative vault, transaction rows, accounts, balances, merchants, reports, provider credentials, or portable-vault bytes;
- review actions perform no fetch, XHR, WebSocket, beacon, browser-storage, cookie, service-worker, or analytics operation;
- optional workflow observations reject likely amounts, account numbers, card details, transaction identifiers, and contact details;
- exported bundles declare manual review only, no telemetry, no financial data, no persistent store, and no remote transmission;
- no automatic workflow removal, consolidation, or feature approval is performed;
- v106 and v107 styles are consolidated into one request so the v129 bootstrap keeps the startup request ceiling unchanged;
- the in-app roadmap contains exactly ten releases from v127 through v136, with v127 and v128 shipped and v129 current;
- only `gringottsBudgetVault.latest` may contain authoritative transaction copies;
- workbook labels, tests, and generated output remain capped at 43 sheets;
- existing export schemas and v126-owned filenames remain unchanged;
- no new finance feature, remote endpoint, service worker, automatic financial action, provider token, cloud upload, persistent store, second runtime, or additional observer is introduced;
- empty-vault protection, backup-first broad writes, immutable close history, guarded import and restore, and stable v105 rescue remain unchanged.
