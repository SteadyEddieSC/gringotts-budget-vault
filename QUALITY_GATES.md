# Quality Gates

A release is promotable only when the exact final head passes every required gate.

## v130 required gates

- locked dependency installation before parser validation;
- strict TypeScript no-emit checking for v128 portable-vault, v129 workflow-evidence, and v130 performance-budget contracts;
- parser and static syntax for inherited runtime modules, shared v129 integration, v130 runtime health, and the flattened v130 bootstrap;
- browser-free budget evaluation, malformed-measurement rejection, local-only, memory-only, and ownership contracts;
- Chromium, Firefox, and desktop WebKit;
- Android Chromium, iPad WebKit, and iPhone WebKit;
- repeated Workflow Review, Roadmap, Diagnostics, and primary-route settlement without detached targets or mutation feedback loops;
- keyboard and visual contracts;
- axe accessibility for desktop and mobile Workflow Review, v130 Diagnostics, and retained household areas;
- Lighthouse ceilings of no more than 45 requests and 500,000 script bytes;
- public-repository security and full-history privacy/secret scanning;
- dependency review and high/critical npm audit;
- supply-chain checks and CodeQL;
- exact-head Cloudflare preview;
- unresolved review-thread check.

## Release assertions

- current version is v130 in both HTML shells, runtime metadata, tests, package metadata, and lockfile metadata;
- TypeScript 5.9.2 remains pinned and strict typechecking runs before browser installation;
- v126 remains the only coordinator for rendered-route enhancement readiness, the only specialist action dispatcher, and the only live `MutationObserver` owner;
- v127 remains the retained interaction, focus, action-language, table-region, dialog, and responsive presentation policy;
- v128 remains the typed portable-vault foundation and adds no encryption or cloud adapter in v130;
- v129 Workflow Review remains manual, session-only, privacy-filtered, and limited to one secondary Tools tab;
- Workflow Review route, field, and action handling are registered through the v126 coordinator and dispatcher;
- the active v130 boot imports v128 directly and does not import `boot-v129.js`; the v129 compatibility entry remains available;
- v130 performance history is bounded to 12 in-memory samples and is cleared by reload;
- performance history reads no authoritative vault, transaction, account, balance, merchant, report, credential, or portable-vault payload;
- no performance or Workflow Review action performs remote logging, analytics, beacon, browser-storage, cookie, service-worker, or cloud operation;
- route-ready time remains capped at 750 ms, enhancement time at 300 ms, enhancement passes at 3, observer callbacks per route at 12, registered releases at 12, and registered actions at 40;
- the in-app roadmap contains exactly ten releases from v127 through v136, with v127–v129 shipped and v130 current;
- only `gringottsBudgetVault.latest` may contain authoritative transaction copies;
- workbook labels, tests, and generated output remain capped at 43 sheets;
- existing export schemas and v126-owned filenames remain unchanged;
- no new finance feature, remote endpoint, service worker, automatic financial action, provider token, cloud upload, persistent store, second runtime, or additional observer is introduced;
- empty-vault protection, backup-first broad writes, immutable close history, guarded import and restore, and stable v105 rescue remain unchanged.
