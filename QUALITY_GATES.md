# Quality Gates

A release is promotable only when the exact final head passes every required gate.

## v131 required gates

- locked dependency installation before parser validation;
- strict TypeScript no-emit checking for v128 portable-vault, v129 workflow-evidence, v130 performance-budget, and v131 decision-gate contracts;
- parser and static syntax for inherited runtime modules, shared v129 integration, retained v130 Diagnostics, lazy v131 integration and UI, and the startup-light v131 bootstrap;
- browser-free review validation, summary consistency, runtime blocking, closed-default states, rationale rejection, decision-record privacy, and no-automatic-approval contracts;
- Chromium, Firefox, and desktop WebKit;
- Android Chromium, iPad WebKit, and iPhone WebKit;
- repeated Decision Gate, Workflow Review, Roadmap, Diagnostics, and primary-route settlement without detached targets or mutation feedback loops;
- keyboard and visual contracts;
- axe accessibility for desktop and mobile Workflow Review, v130 Diagnostics, v131 Decision Gate, and retained household areas;
- Lighthouse ceilings of no more than 45 startup requests and 500,000 startup script bytes;
- public-repository security and full-history privacy/secret scanning;
- dependency review and high/critical npm audit;
- supply-chain checks and CodeQL;
- exact-head Cloudflare preview;
- unresolved review-thread check.

## Release assertions

- current version is v131 in both HTML shells, runtime metadata, tests, package metadata, and lockfile metadata;
- TypeScript 5.9.2 remains pinned and strict typechecking runs before browser installation;
- v126 remains the only coordinator for rendered-route enhancement readiness, the only specialist action dispatcher, and the only live `MutationObserver` owner;
- v127 remains the retained interaction, focus, action-language, table-region, dialog, and responsive presentation policy;
- v128 remains the typed portable-vault foundation and adds no encryption or cloud adapter in v131;
- the active v131 boot statically imports v128 only; it does not statically import the v129 integration, v130 boot, v130 Diagnostics, v131 integration, v131 UI, or decision contracts;
- the v129 and v130 historical boot entries remain available but are not loaded by the v131 shells;
- Workflow Review and Decision Gate integration load only after Tools opens and register route or field actions through the v126 dispatcher;
- the Decision Gate UI and decision contracts load only after Decision Gate opens;
- v130 Diagnostics and the performance evaluator remain lazy until Diagnostics or explicit runtime evaluation;
- v129 Workflow Review remains manual, session-only, privacy-filtered, and limited to one secondary Tools tab;
- v131 imports only an explicitly selected v129 review bundle and validates kind, version, inventory, privacy, unique known workflow IDs, and summary consistency;
- v131 reads maintenance evidence only through `window.GringottsV130.snapshot()` and remains closed when review or runtime evidence is incomplete;
- `candidate-proposal` permits one later proposal only and never approves or implements a capability;
- Decision Gate state is bounded to module memory and cleared by reload;
- Workflow Review, runtime evidence, and Decision Gate read no authoritative vault, transaction, account, balance, merchant, report, credential, or portable-vault payload;
- no v131 action performs remote logging, analytics, beacon, browser-storage, cookie, service-worker, cloud, or automatic financial operation;
- route-ready time remains capped at 750 ms, enhancement time at 300 ms, enhancement passes at 3, observer callbacks per route at 12, registered releases at 12, and registered actions at 40;
- startup remains capped at 45 requests and 500,000 script bytes without weakening Lighthouse assertions;
- the in-app roadmap contains exactly ten releases from v127 through v136, with v127–v130 shipped and v131 current;
- only `gringottsBudgetVault.latest` may contain authoritative transaction copies;
- workbook labels, tests, and generated output remain capped at 43 sheets;
- existing financial export schemas and v126-owned filenames remain unchanged;
- no new finance feature, remote endpoint, service worker, automatic financial action, provider token, cloud upload, persistent store, second runtime, additional observer, or report sheet is introduced;
- empty-vault protection, backup-first broad writes, immutable close history, guarded import and restore, and stable v105 rescue remain unchanged.