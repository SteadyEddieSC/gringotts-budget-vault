# Import profile schema

Profiles contain a profile ID, user name, format, schema ID and label, delimiter, non-reversible ordered-header signature, header count, mapped header names, normalization options, and timestamps. Profiles never contain source rows, file content, filenames, source fingerprints, or transaction arrays.
